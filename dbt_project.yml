name: governed_metrics
version: '1.0.0'
config-version: 2

profile: governed_metrics

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  governed_metrics:
    staging:
      +materialized: view
    marts:
      +materialized: table
